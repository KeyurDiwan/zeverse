{
  "mcpServers": {
    "freshrelease": {
      "type": "stdio",
      "command": "node",
      "args": ["${userHome}/.claude/mcp-servers/freshrelease/index.js"],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
